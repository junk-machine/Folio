<?xml version="1.0" encoding="utf-8"?>
<!-- Separate resource for RTL layout, since autoMirrored flag doesn't work -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners
        android:topRightRadius="@dimen/widget_corner_radius"
        android:bottomRightRadius="@dimen/widget_corner_radius" />

    <solid android:color="@color/widgetBackgroundMask" />
</shape>